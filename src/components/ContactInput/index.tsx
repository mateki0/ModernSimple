import * as React from 'react';

interface InputProps {
  width?: string;
  height?: string;
  label: string;
  name: string;
}
const ContactInput: React.FC<InputProps> = ({ width, height, label, name }) => {
  return <div></div>;
};
export default ContactInput;
