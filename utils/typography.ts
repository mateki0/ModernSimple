import Typography from 'typography';

const typography = new Typography({
  headerFontFamily: ['Nunito-black', 'sans-serif'],
  bodyFontFamily: ['Nunito', 'sans-serif'],
});
typography.injectStyles();
export default typography;
